export default async function handler(req, res) {
  const { method } = req;
  // const {} = req.body;
  // const {} = req.query;

  switch (method) {
    case 'GET':
      try {
        // logic goes here
      } catch (error) {
        res.status(400).json({ success: false });
      }
      break;
    case 'POST':
      try {
        // logic goes here
      } catch (error) {
        res.status(400).json({ success: false });
      }
      break;
    default:
      res.status(400).json({ success: false });
      break;
  }
}
