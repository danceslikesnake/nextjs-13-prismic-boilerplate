import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { fa{{properCase name}} as IconSolid } from '@fortawesome/pro-solid-svg-icons';
import { fa{{properCase name}} as IconRegular } from '@fortawesome/pro-regular-svg-icons';
import { fa{{properCase name}} as IconLight } from '@fortawesome/pro-light-svg-icons';
import { fa{{properCase name}} as IconThin } from '@fortawesome/pro-thin-svg-icons';
import { fa{{properCase name}} as IconDuotone } from '@fortawesome/pro-duotone-svg-icons';
import { fa{{properCase name}} as IconSharp } from '@fortawesome/sharp-solid-svg-icons';
import PropTypes from 'prop-types';

const Fa{{properCase name}} = ({ style, spin, beat, fade, rotation, flip }) => {
  const renderIcon = () => {
    switch (style) {
      case 'duotone':
        return (
          <FontAwesomeIcon
            icon={IconDuotone}
            spin={spin}
            beat={beat}
            fade={fade}
            rotation={rotation}
            flip={flip}
          />
        );
      case 'thin':
        return (
          <FontAwesomeIcon
            icon={IconThin}
            spin={spin}
            beat={beat}
            fade={fade}
            rotation={rotation}
            flip={flip}
          />
        );
      case 'light':
        return (
          <FontAwesomeIcon
            icon={IconLight}
            spin={spin}
            beat={beat}
            fade={fade}
            rotation={rotation}
            flip={flip}
          />
        );
      case 'regular':
        return (
          <FontAwesomeIcon
            icon={IconRegular}
            spin={spin}
            beat={beat}
            fade={fade}
            rotation={rotation}
            flip={flip}
          />
        );
      case 'sharp':
      return (
          <FontAwesomeIcon
            icon={IconRegular}
            spin={spin}
            beat={beat}
            fade={fade}
            rotation={rotation}
            flip={flip}
          />
        );
      case 'solid':
      default:
        return (
          <FontAwesomeIcon
            icon={IconSolid}
            spin={spin}
            beat={beat}
            fade={fade}
            rotation={rotation}
            flip={flip}
          />
        );
    }
  };
  return renderIcon();
};

Fa{{properCase name}}.propTypes = {
  spin: PropTypes.bool,
};

export default Fa{{properCase name}};
